# Copy this file to `.env` and fill in the two secrets. .env is gitignored.

# MongoDB Atlas connection string. Cluster: `yaikhhomepage`,
# DB name (defaulted in code): `yaikh`. Same cluster the yaikh.com marketing
# site reads from — do NOT create a new database, we want a single source of truth.
MONGO_URL="mongodb+srv://<user>:<pass>@yaikhhomepage.pt9xtbm.mongodb.net/?retryWrites=true&w=majority&appName=yaikhhomepage"

# Google AI Studio key on a Cloud Billing account with the Free Trial credit
# still active (create at https://aistudio.google.com/apikey; pick
# "Default Gemini Project" during creation so the credit is charged).
GEMINI_API_KEY="AQ.Ab8RN6K..."
